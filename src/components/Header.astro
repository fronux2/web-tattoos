<nav class="flex justify-between items-center py-5 border-b border-hairline px-4 sm:px-8 lg:px-16">
  <div class="flex items-center">
    <a href="/" class="font-display text-xl font-semibold tracking-tight flex items-center gap-1">
      <span>MARCO<span class="text-brass">INK</span></span>
    </a>
  </div>

  <button id="nav-toggle" aria-controls="nav-list" aria-expanded="false" class="sm:hidden p-2 rounded-[3px] border border-hairline-strong text-text-primary">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.4">
      <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>

  <ul id="nav-list" class="hidden sm:flex sm:items-center sm:space-x-8 flex-col sm:flex-row space-y-1 sm:space-y-0 mt-4 sm:mt-0 w-full sm:w-auto bg-ink-surface sm:bg-transparent p-4 sm:p-0 rounded-[3px] sm:rounded-none border border-hairline sm:border-0">
    <li>
      <a href="/" class="block w-full text-center sm:text-left px-1 py-2 font-mono text-[12px] uppercase tracking-[0.1em] text-text-secondary hover:text-brass-bright transition-colors">Inicio</a>
    </li>
    <li>
      <a href="/about" class="block w-full text-center sm:text-left px-1 py-2 font-mono text-[12px] uppercase tracking-[0.1em] text-text-secondary hover:text-brass-bright transition-colors">Sobre Mí</a>
    </li>
    <li>
      <a href="/gallery" class="block w-full text-center sm:text-left px-1 py-2 font-mono text-[12px] uppercase tracking-[0.1em] text-text-secondary hover:text-brass-bright transition-colors">Galería</a>
    </li>
    <li>
      <a href="/contact" class="block w-full text-center sm:text-left px-4 py-2 rounded-[3px] border border-hairline-strong font-mono text-[12px] uppercase tracking-[0.1em] text-brass-bright hover:bg-ink-elevated transition-colors">Reservar</a>
    </li>
  </ul>

  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const btn = document.getElementById('nav-toggle');
      const nav = document.getElementById('nav-list');
      if (!btn || !nav) return;
      btn.addEventListener('click', function () {
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', String(!expanded));
        nav.classList.toggle('hidden');
      });
    });
  </script>
</nav>
